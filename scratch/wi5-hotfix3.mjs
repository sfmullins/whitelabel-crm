import fs from 'node:fs';
function replace(path,search,replacement){const value=fs.readFileSync(path,'utf8');if(value.includes(replacement))return;if(!value.includes(search))throw new Error(`Missing WI5 hotfix3 target in ${path}: ${search}`);fs.writeFileSync(path,value.replace(search,replacement));}
replace('frontend/src/lib/wi4.ts',`  const order: SearchEntityType[] = ['organisation', 'contact', 'engagement', 'activity', 'customer', 'invoice'];`,`  const order: SearchEntityType[] = ['organisation', 'contact', 'engagement', 'activity', 'task', 'document', 'communication', 'customer', 'invoice'];`);
replace('frontend/src/lib/wi4.ts',`    invoice: 'Invoices',\n  } as const)[type];`,`    invoice: 'Invoices',\n    task: 'Tasks',\n    document: 'Documents',\n    communication: 'Communications',\n  } as const)[type];`);
replace('frontend/src/pages/SearchResults.tsx',`import { Building2, FileText, MessageSquare, Search, UserRound, BriefcaseBusiness, Users } from 'lucide-react';`,`import { Building2, CheckSquare, FileText, FolderOpen, MessageSquare, Radio, Search, UserRound, BriefcaseBusiness, Users } from 'lucide-react';`);
replace('frontend/src/pages/SearchResults.tsx',`const types: SearchEntityType[] = ['organisation','contact','engagement','activity','customer','invoice'];`,`const types: SearchEntityType[] = ['organisation','contact','engagement','activity','task','document','communication','customer','invoice'];`);
replace('frontend/src/pages/SearchResults.tsx',`const icons = { organisation: Building2, contact: UserRound, engagement: BriefcaseBusiness, activity: MessageSquare, customer: Users, invoice: FileText };`,`const icons = { organisation: Building2, contact: UserRound, engagement: BriefcaseBusiness, activity: MessageSquare, task: CheckSquare, document: FolderOpen, communication: Radio, customer: Users, invoice: FileText };`);
console.log('Applied WI5 search UI hotfixes.');
