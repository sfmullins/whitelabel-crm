import fs from 'node:fs';

function read(path){return fs.readFileSync(path,'utf8');}
function write(path,value){fs.writeFileSync(path,value);}
function replace(path,search,replacement){const value=read(path);if(!value.includes(search))throw new Error(`Missing WI5 patch target in ${path}: ${search.slice(0,120)}`);write(path,value.replace(search,replacement));}

replace('backend/src/config/runtimePaths.ts',`  logDirectory: string;\n}`,`  logDirectory: string;\n  documentDirectory: string;\n}`);
replace('backend/src/config/runtimePaths.ts',`  logDirectory: path.join(defaultDataDir, 'logs')\n};`,`  logDirectory: path.join(defaultDataDir, 'logs'),\n  documentDirectory: path.join(defaultDataDir, 'documents')\n};`);
replace('backend/src/config/runtimePaths.ts',`    activePaths.logDirectory\n  ];`,`    activePaths.logDirectory,\n    activePaths.documentDirectory\n  ];`);

replace('backend/src/infrastructure/database/migrate.ts',`import { rebuildSearchIndex } from './WorkspaceRepository';`,`import { rebuildSearchIndex } from './WorkspaceRepository';\nimport { ensureOperationalSchema } from './operationalSchema';`);
replace('backend/src/infrastructure/database/migrate.ts',`  migrate(dbInstance, { migrationsFolder: folder });\n  runWi3LegacyActivityBackfill(sqliteConnection);`,`  migrate(dbInstance, { migrationsFolder: folder });\n  ensureOperationalSchema(sqliteConnection);\n  runWi3LegacyActivityBackfill(sqliteConnection);`);

replace('backend/src/presentation/app.ts',`import workspaceRouter from './routes/workspace';`,`import workspaceRouter from './routes/workspace';\nimport operationalRouter from './routes/operational';`);
replace('backend/src/presentation/app.ts',`app.use('/api', workspaceRouter);`,`app.use('/api', workspaceRouter);\napp.use('/api', operationalRouter);`);

replace('frontend/src/routes/index.tsx',`import SearchResults from '../pages/SearchResults';`,`import SearchResults from '../pages/SearchResults';\nimport Work from '../pages/Work';\nimport Documents from '../pages/Documents';\nimport Communications from '../pages/Communications';\nimport Automation from '../pages/Automation';`);
replace('frontend/src/routes/index.tsx',`    { path: 'search', element: <SearchResults /> },`,`    { path: 'search', element: <SearchResults /> },\n    { path: 'work', element: <Work /> },\n    { path: 'documents', element: <Documents /> },\n    { path: 'communications', element: <Communications /> },\n    { path: 'automation', element: <Automation /> },`);

replace('frontend/src/components/layouts/MainLayout.tsx',`  BriefcaseBusiness, Building2, Calendar, CalendarClock, FileText, LayoutDashboard,\n  Layers, Plus, Search, Settings, UserRound, Users, X,`,`  BriefcaseBusiness, Building2, Calendar, CalendarClock, CheckSquare, FileText, FolderOpen, LayoutDashboard,\n  Layers, Plus, Radio, Search, Settings, UserRound, Users, Workflow, X,`);
replace('frontend/src/components/layouts/MainLayout.tsx',`  { label: 'Operations', items: [\n    { to: '/customers', label: 'Customer records', icon: Users },`,`  { label: 'Operations', items: [\n    { to: '/work', label: 'Work', icon: CheckSquare },\n    { to: '/documents', label: 'Documents', icon: FolderOpen },\n    { to: '/communications', label: 'Communications', icon: Radio },\n    { to: '/automation', label: 'Automation', icon: Workflow },\n    { to: '/customers', label: 'Customer records', icon: Users },`);
replace('frontend/src/components/layouts/MainLayout.tsx',`Search organisations, contacts, activities and invoices…`,`Search organisations, contacts, activities, tasks, documents and communications…`);
replace('frontend/src/components/layouts/MainLayout.tsx',`    { title: 'Open follow-up queue', route: '/follow-ups' },`,`    { title: 'Open follow-up queue', route: '/follow-ups' },\n    { title: 'Create task', route: '/work?action=create' },\n    { title: 'Upload document', route: '/documents?action=upload' },\n    { title: 'Log communication', route: '/communications?action=create' },`);

replace('package.json',`    "ci:verify": "npm run build && npm test && npm run db:smoke && npm run wi4:smoke && npm run desktop:preflight",\n    "wi4:smoke": "npm exec -w backend -- tsx ../scratch/wi4-smoke.ts"`,`    "ci:verify": "npm run build && npm test && npm run db:smoke && npm run wi4:smoke && npm run wi5:smoke && npm run desktop:preflight",\n    "wi4:smoke": "npm exec -w backend -- tsx ../scratch/wi4-smoke.ts",\n    "wi5:smoke": "npm exec -w backend -- tsx ../scratch/wi5-smoke.ts"`);

replace('shared/src/types.ts',`  'customer',\n  'invoice',\n]);`,`  'customer',\n  'invoice',\n  'task',\n  'document',\n  'communication',\n]);`);
replace('shared/src/types.ts',`  types: z.array(SearchEntityTypeSchema).max(6).optional(),`,`  types: z.array(SearchEntityTypeSchema).max(9).optional(),`);
replace('backend/src/infrastructure/database/schema.ts',`sql\`\${table.entityType} in ('organisation', 'contact', 'engagement', 'activity', 'customer', 'invoice')\``,`sql\`\${table.entityType} in ('organisation', 'contact', 'engagement', 'activity', 'customer', 'invoice', 'task', 'document', 'communication')\``);
replace('backend/drizzle/0003_needy_carmella_unuscione.sql',`CHECK(\`entity_type\` in ('organisation','contact','engagement','activity','customer','invoice'))`,`CHECK(\`entity_type\` in ('organisation','contact','engagement','activity','customer','invoice','task','document','communication'))`);

replace('backend/src/infrastructure/database/WorkspaceRepository.ts',`  'customer',\n  'invoice',\n] as const;`,`  'customer',\n  'invoice',\n  'task',\n  'document',\n  'communication',\n] as const;`);
replace('backend/src/infrastructure/database/WorkspaceRepository.ts',`      left join organisations o on o.id = m.organisation_id\n    \`).run();\n  })();`,`      left join organisations o on o.id = m.organisation_id\n    \`).run();\n\n    connection.prepare(\`\n      insert into search_documents(id,entity_type,entity_id,organisation_id,title,subtitle,body,route,updated_at,archived_at)\n      select 'task:' || id,'task',id,organisation_id,title,upper(priority) || ' · ' || replace(status,'_',' '),\n        coalesce(description,''),'/work?taskId=' || id,updated_at,archived_at from tasks\n    \`).run();\n    connection.prepare(\`\n      insert into search_documents(id,entity_type,entity_id,organisation_id,title,subtitle,body,route,updated_at,archived_at)\n      select 'document:' || d.id,'document',d.id,\n        (select entity_id from document_links where document_id=d.id and entity_type='organisation' limit 1),\n        d.title,d.current_filename || ' · ' || d.mime_type,trim(coalesce(d.description,'') || ' ' || coalesce(d.category,'')),\n        '/documents?documentId=' || d.id,d.updated_at,d.archived_at from documents d\n    \`).run();\n    connection.prepare(\`\n      insert into search_documents(id,entity_type,entity_id,organisation_id,title,subtitle,body,route,updated_at,archived_at)\n      select 'communication:' || id,'communication',id,organisation_id,\n        coalesce(nullif(trim(subject),''),upper(substr(channel,1,1)) || substr(channel,2)),\n        upper(direction) || ' · ' || channel,body,'/communications?communicationId=' || id,updated_at,archived_at\n      from communications\n    \`).run();\n  })();`);

replace('backend/src/test/crm/helpers.ts',`import { closeDatabase, openDatabase, sqlite } from '../../infrastructure/database/connection';`,`import { closeDatabase, openDatabase, sqlite } from '../../infrastructure/database/connection';\nimport { configureRuntimePaths } from '../../config/runtimePaths';`);
replace('backend/src/test/crm/helpers.ts',`  const database = openDatabase(path.join(tempDir, 'test.sqlite'));`,`  const databasePath = path.join(tempDir, 'test.sqlite');\n  configureRuntimePaths({ dataDirectory: tempDir, databasePath, internalBackupDirectory: path.join(tempDir, 'backups'), temporaryDirectory: path.join(tempDir, 'tmp'), logDirectory: path.join(tempDir, 'logs'), documentDirectory: path.join(tempDir, 'documents') });\n  const database = openDatabase(databasePath);`);

replace('backend/src/infrastructure/database/seed.ts',`import { rebuildSearchIndex } from './WorkspaceRepository';`,`import { rebuildSearchIndex } from './WorkspaceRepository';\nimport { LocalDocumentStorage } from '../storage/LocalDocumentStorage';`);
replace('backend/src/infrastructure/database/seed.ts',`  acmeInvoiceItem: '20000000-0000-4000-8000-000000000017',`,`  acmeInvoiceItem: '20000000-0000-4000-8000-000000000017',\n  acmeTask: '20000000-0000-4000-8000-000000000018',\n  acmeCompletedTask: '20000000-0000-4000-8000-000000000019',\n  acmeReminder: '20000000-0000-4000-8000-000000000020',\n  acmeDocument: '20000000-0000-4000-8000-000000000021',\n  acmeDocumentVersion: '20000000-0000-4000-8000-000000000022',\n  acmeDocumentLink: '20000000-0000-4000-8000-000000000023',\n  acmeEmailCommunication: '20000000-0000-4000-8000-000000000024',\n  acmeMeetingCommunication: '20000000-0000-4000-8000-000000000025',\n  acmeWorkflow: '20000000-0000-4000-8000-000000000026',\n  acmeWorkflowRun: '20000000-0000-4000-8000-000000000027',\n  acmeWorkflowActionRun: '20000000-0000-4000-8000-000000000028',`);
replace('backend/src/infrastructure/database/seed.ts',`  sqlite.transaction(() => {`,`  sqlite.exec(\`\n    DELETE FROM workflow_action_runs; DELETE FROM workflow_runs; DELETE FROM workflow_definitions;\n    DELETE FROM document_links; DELETE FROM document_versions; DELETE FROM documents;\n    DELETE FROM reminders; DELETE FROM tasks; DELETE FROM communications;\n  \`);\n\n  sqlite.transaction(() => {`);
replace('backend/src/infrastructure/database/seed.ts',`  rebuildSearchIndex(sqlite);\n  console.log(\`WI4 seed complete. Acme Ltd is ready; today is \${today}.\`);`,`  const storage = new LocalDocumentStorage();\n  const content = Buffer.from('Acme Ltd diagnostic proposal and evidence summary.','utf8');\n  const stored = storage.write(IDS.acmeDocument,IDS.acmeDocumentVersion,'acme-diagnostic-proposal.txt',content);\n  sqlite.transaction(() => {\n    sqlite.prepare(\`INSERT INTO tasks(id,organisation_id,contact_id,engagement_id,activity_id,source_type,source_id,title,description,status,priority,due_at,reminder_at,recurrence_rule,assigned_to,created_by_source,workflow_run_id,completed_at,created_at,updated_at,archived_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,NULL)\`).run(IDS.acmeTask,IDS.acmeOrganisation,IDS.acmeAisling,IDS.acmeDiagnostic,null,'engagement',IDS.acmeDiagnostic,'Prepare leadership readout','Consolidate diagnostic evidence and prepare the leadership readout.','in_progress','high',upcomingDate+'T10:00:00.000Z',today+'T16:00:00.000Z',null,'Stephen Mullins','user',null,null,now,now);\n    sqlite.prepare(\`INSERT INTO tasks(id,organisation_id,contact_id,engagement_id,title,description,status,priority,due_at,created_by_source,completed_at,created_at,updated_at,archived_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,NULL)\`).run(IDS.acmeCompletedTask,IDS.acmeOrganisation,IDS.acmeAisling,IDS.acmeDiagnostic,'Issue evidence request','Evidence request issued and acknowledged.','completed','normal',overdueDate+'T09:00:00.000Z','user',overdueDate+'T16:00:00.000Z',overdueDate+'T09:00:00.000Z',now);\n    sqlite.prepare(\`INSERT INTO reminders(id,source_type,source_id,organisation_id,scheduled_at,delivery_method,status,created_at,updated_at) VALUES(?,?,?,?,?,'desktop','pending',?,?)\`).run(IDS.acmeReminder,'task',IDS.acmeTask,IDS.acmeOrganisation,today+'T16:00:00.000Z',now,now);\n    sqlite.prepare(\`INSERT INTO documents(id,title,current_filename,mime_type,byte_size,checksum,storage_provider,storage_key,description,category,created_at,updated_at,archived_at) VALUES(?,?,?,?,?,?,'local',?,?,?, ?,?,NULL)\`).run(IDS.acmeDocument,'Acme diagnostic proposal','acme-diagnostic-proposal.txt','text/plain',stored.byteSize,stored.checksum,stored.storageKey,'Proposal and evidence summary for the active diagnostic.','proposal',now,now);\n    sqlite.prepare(\`INSERT INTO document_versions(id,document_id,version_number,filename,mime_type,byte_size,checksum,storage_key,version_note,created_at) VALUES(?,?,1,?,?,?,?,?,'Initial WI5 fixture',?)\`).run(IDS.acmeDocumentVersion,IDS.acmeDocument,'acme-diagnostic-proposal.txt','text/plain',stored.byteSize,stored.checksum,stored.storageKey,now);\n    sqlite.prepare(\`INSERT INTO document_links(id,document_id,entity_type,entity_id,created_at) VALUES(?,?,'organisation',?,?)\`).run(IDS.acmeDocumentLink,IDS.acmeDocument,IDS.acmeOrganisation,now);\n    sqlite.prepare(\`INSERT INTO communications(id,organisation_id,contact_id,engagement_id,channel,direction,subject,body,occurred_at,status,created_at,updated_at,archived_at) VALUES(?,?,?,?,?,?,?,?,?,'logged',?,?,NULL)\`).run(IDS.acmeEmailCommunication,IDS.acmeOrganisation,IDS.acmeAisling,IDS.acmeDiagnostic,'email','inbound','Diagnostic evidence','Aisling confirmed the evidence pack is complete.',addDays(nowDate,-2)+'T11:00:00.000Z',now,now);\n    sqlite.prepare(\`INSERT INTO communications(id,organisation_id,contact_id,engagement_id,channel,direction,subject,body,occurred_at,status,created_at,updated_at,archived_at) VALUES(?,?,?,?,?,?,?,?,?,'logged',?,?,NULL)\`).run(IDS.acmeMeetingCommunication,IDS.acmeOrganisation,IDS.acmeAisling,IDS.acmeDiagnostic,'meeting','internal','Leadership review','Reviewed initial findings and agreed the leadership readout.',addDays(nowDate,-1)+'T14:00:00.000Z',now,now);\n    sqlite.prepare(\`INSERT INTO workflow_definitions(id,name,description,enabled,version,trigger_type,condition_json,action_json,created_at,updated_at,archived_at) VALUES(?,?,?,1,1,'manual','{}',?,?,?,NULL)\`).run(IDS.acmeWorkflow,'Post-meeting follow-up','Create a follow-up task after a meeting.',JSON.stringify([{type:'create_task',title:'Send meeting follow-up',priority:'high'}]),now,now);\n    sqlite.prepare(\`INSERT INTO workflow_runs(id,workflow_definition_id,workflow_version,source_type,source_id,trigger_event,idempotency_key,status,output_summary,started_at,completed_at) VALUES(?, ?,1,'organisation',?,'seed','wi5-seed-workflow','succeeded','[]',?,?)\`).run(IDS.acmeWorkflowRun,IDS.acmeWorkflow,IDS.acmeOrganisation,now,now);\n    sqlite.prepare(\`INSERT INTO workflow_action_runs(id,workflow_run_id,action_index,action_type,status,output_json,started_at,completed_at) VALUES(?,?,0,'create_task','succeeded','{}',?,?)\`).run(IDS.acmeWorkflowActionRun,IDS.acmeWorkflowRun,now,now);\n  })();\n\n  rebuildSearchIndex(sqlite);\n  console.log(\`WI5 seed complete. Acme Ltd operational records are ready; today is \${today}.\`);`);

replace('frontend/src/pages/OrganisationWorkspace.tsx',`import { Archive, BriefcaseBusiness, Building2, ExternalLink, Mail, MessageSquare, Pencil, Star, UserRound } from 'lucide-react';`,`import { Archive, BriefcaseBusiness, Building2, CheckSquare, ExternalLink, FolderOpen, Mail, MessageSquare, Pencil, Radio, Star, UserRound } from 'lucide-react';`);
replace('frontend/src/pages/OrganisationWorkspace.tsx',`type Tab = 'overview' | 'timeline' | 'contacts' | 'engagements';`,`type Tab = 'overview' | 'timeline' | 'contacts' | 'engagements' | 'work' | 'documents' | 'communications';`);
replace('frontend/src/pages/OrganisationWorkspace.tsx',`{(['overview','timeline','contacts','engagements'] as Tab[])`,`{(['overview','timeline','contacts','engagements','work','documents','communications'] as Tab[])`);
replace('frontend/src/pages/OrganisationWorkspace.tsx',`    {tab === 'engagements' && <EngagementsView data={data}/>} `,`    {tab === 'engagements' && <EngagementsView data={data}/>} \n    {tab === 'work' && <OperationalSection organisationId={organisationId} kind="work"/>}\n    {tab === 'documents' && <OperationalSection organisationId={organisationId} kind="documents"/>}\n    {tab === 'communications' && <OperationalSection organisationId={organisationId} kind="communications"/>} `);
replace('frontend/src/pages/OrganisationWorkspace.tsx',`function OrganisationForm(`,`function OperationalSection({ organisationId,kind }: { organisationId:string;kind:'work'|'documents'|'communications' }) {\n  const config = kind === 'work' ? { icon: CheckSquare,title:'Work and reminders',text:'Open the consolidated queue filtered to this organisation.' } : kind === 'documents' ? { icon: FolderOpen,title:'Documents and attachments',text:'Open the local versioned document library.' } : { icon: Radio,title:'Communications',text:'Open email, meeting, call and future-channel records.' };\n  const Icon=config.icon;\n  return <section className="rounded-xl border bg-card p-8 shadow-sm"><div className="flex items-start gap-4"><div className="flex h-12 w-12 items-center justify-center rounded-xl bg-primary/10 text-primary"><Icon className="h-6 w-6"/></div><div><h2 className="text-xl font-bold">{config.title}</h2><p className="mt-1 text-sm text-muted-foreground">{config.text}</p><Link className="mt-4 inline-flex h-10 items-center rounded-md bg-primary px-4 text-sm font-bold text-primary-foreground" to={\`/\${kind}?organisationId=\${organisationId}\`}>Open {kind}</Link></div></div></section>;\n}\n\nfunction OrganisationForm(`);

console.log('Applied WI5 integration patches.');
