name: WI8-WI9 diagnostics

on:
  push:
    branches:
      - WI8-WI9-reporting-multiuser-hardening
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: wi8-wi9-diagnostics-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Capture build output
        id: build
        shell: bash
        run: |
          set +e
          npm run build 2>&1 | tee wi8-wi9-build.log
          status=${PIPESTATUS[0]}
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Upload build diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wi8-wi9-build-log
          path: wi8-wi9-build.log
          if-no-files-found: error
      - name: Report build result
        run: exit "${{ steps.build.outputs.status }}"
